let scores = [50, 20, 70, 10, 40];

// Ascending order
scores.sort((a, b) => a - b)
console.log(scores)

// Descending order
scores.sort((a, b) => b - a)
console.log(scores)

// ----------------------------------------

let names = ["Shatha", "Sara", "Lina", "Sami", "Dalia"];

names.sort();
console.log(names)